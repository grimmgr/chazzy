import React from 'react';
import { ContactList } from '../components/ContactList';

export const ContactPage = () => {
    return (
        <section id='contact'>
            <h2>Contact</h2>
            <ContactList />
        </section>
    );
};