import React from 'react';
import { NoShows } from '../components/NoShows';

export const ShowsPage = () => {
    return (
        <section id='shows'>
            <h2>SHOWS</h2>
            <NoShows />
        </section>
    );
};